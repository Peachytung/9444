# Final QA report

Overall: **PENDING**

The project configuration has been updated to the 3,000/500 split, 30 epochs,
384 x 320 inputs, batch size 2, learning rate 3e-4 and dropout 0.2. Previous
QA results for the older run are historical only.

Re-run after training:

```bash
python scripts/final_qa.py
```
