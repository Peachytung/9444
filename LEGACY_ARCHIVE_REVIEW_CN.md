# 旧“归档”压缩包审查与替换说明

旧压缩包可以作为早期草稿参考，但不适合直接提交。已经发现并在本文件夹中修正的问题如下：

1. **测试集泄漏**：旧流程直接把官方 `test1000` 当作 validation，并用它选择 best model，导致最终测试结果不再独立。新实现从 `train9000` 内部划出 validation，官方 test 只在训练和选模结束后使用一次。
2. **VGG16 缺少可核验证据**：旧材料里有 VGG16 的文字描述，但没有对应 checkpoint、history、训练/验证曲线、混淆矩阵和逐类指标。新实现为两个模型输出完全对称的一套证据。
3. **旧结果不是当前固定实验**：旧 ResNet 记录主要来自小样本、少 epoch 或低分辨率测试，不能代表当前 baseline。新实现使用 3,000 张训练图、500 张验证图和最终 1,000 张官方测试图。
4. **模型定义和任务输出不完整**：项目要求 103 类像素级语义分割，不是普通图像分类。新实现使用 ResNet50/VGG16 编码器加简单 FCN segmentation head，输出与 mask 同尺寸的 103 类 logits。
5. **交付物不齐**：旧压缩包没有完整的 best checkpoint、配置、环境信息、比较表、逐类结果和定性预测。新流程已把这些纳入自动训练和最终 QA。

最终应使用本文件夹的新代码和 HPC2 结果；不要把旧压缩包中的数值混入新报告或 PPT。
